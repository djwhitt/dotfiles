#!/usr/bin/env bash

export VISUAL="$HOME/.local/bin/e"

nnn="$HOME/.local/bin/nnn"

window_class="kitty-nnn"
if ! wmctrl -x -a $window_class; then 
  kitty --class $window_class --title nnn --detach $nnn -e -E
  while ! wmctrl -x -a $window_class; do sleep 0.1; done
fi
